# Examples agents

- Cell 1 = all imports (stdlib/third-party, then `nlp_shap`)
- Full public usability scope; no stubs
- Catalog in `examples/README.md`, `docs/examples.rst`, and root `README.md`
- `make notebooks` before commit (outputs stored)

See `.cursor/rules/examples.mdc` and skill `nlp-shap-notebook`.
