# Backend agents

- Extras only for torch/transformers/lmstudio/httpx
- CI = mock; local markers `lms` / `gpu`
- New backend → entry point + tests + optional notebook/bench

See `.cursor/rules/backends.mdc` and skill `nlp-shap-plugin`.
