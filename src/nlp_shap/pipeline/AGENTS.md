# Pipeline agents

- `ExplainConfig` / YAML is the user-facing entry; keep wire fields typed
- User-facing docs: shipped API only — no rewrite phase numbers
- New public workflow → notebook + docs (`examples.mdc`, `docs.mdc`)

See skill `nlp-shap-development`.
