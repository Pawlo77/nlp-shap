# Docs agents

- User-facing only — shipped API, no rewrite phases
- Theory ↔ guides ↔ API cross-links
- Runnable guides need ``.. guide-result::``; regenerate via `make docs`
- Release notes: dated section per version; clear `Unreleased` on tag

See `.cursor/rules/docs.mdc`.
