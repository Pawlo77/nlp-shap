# Tests agents

- `test_*.py`; mirror `src/nlp_shap/`
- Absolute `from nlp_shap...`
- Default agent run: `make agent-check` / `make tests`
- Full markers: `make tests-all`
- `lms` / `gpu` / `bench` are local/optional

See `.cursor/rules/tests.mdc` and `tdd.mdc`.
